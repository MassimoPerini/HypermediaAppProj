<% console.log(service); %>
<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
    <%- include('../common/imports') %>

  <style>
    header{
      background-image: url('/public/imgs/bg/micro.jpg');
    }

  </style>
</head>
<body>
    <%- include('../common/navbar', { page : '/doctor', user:user}) %>

  <header>
  </header>

  <section class="two-sided" id="content">
    <center>
      <h1><%=service.name%></h1>
    </center>
    <article>
      <div>
        <h2><%= title %></h2>
        <p>Here you can find the list of all doctors operating in this service. Feel free to contact them.</p>
        <div class="row">
        <% var i = 0; service.doctors.forEach(doctor => { %>
            <div class="col-sm-2">
                <div class="card">
                    <a href="/doctor/<%= doctor.id %>">
                        <img class="card-image" src="<%= doctor.icon %>"/>
                        <h5 class="card-text"><%= doctor.fullname %></h5>
                    </a>
                </div>
            </div>
        <% i++; if(i % 3 == 0 && i < service.doctors_services.length) { %>
            </div>
            <div class="row">
        <%   }
           }); %>
        </div>
      </div>

    </article>

    <%- include('sidebar', { page : 'operating-doctors' }) %>

  </section>

<%- include('../common/footer') %>
</body>
</html>
